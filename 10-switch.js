//Switch es una forma diferente de validar,esta depende del caso que sea verdad.

var numero = 1;
var resultado;

/* Switch compara con un === por esatazon los elementos tiene que ser identicos
 */

switch (number) {
    case 1:
    outcome= "Si es uno";
        break;
    case 10:
        outcome= "Si es diez";
        break;
    case 100:
        outcome= "Si es match";
        break;
    default:
        outcome ="No match"
}

console.log(outcome);